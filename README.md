# BMW Connected — Lead Deck

Self-contained HTML/CSS/JS slide deck. Static site, no build step.

## Deploy (Vercel)
Zero-config: `index.html` is at the repo root. Import the repo in Vercel,
framework preset **Other**, no build command, output dir = root.
